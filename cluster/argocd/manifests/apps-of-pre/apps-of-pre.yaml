apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: apps-of-pre
spec:
  destination:
    namespace: argocd
    server: https://kubernetes.default.svc
  source:
    path: cluster/apps/app-of-apps/pre/
    repoURL: https://github.com/AlvaroNieto/homelab/
    targetRevision: pre
  sources: []
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: false
    syncOptions:
      - PruneLast=true
      - CreateNamespace=true
